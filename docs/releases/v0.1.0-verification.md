# AEON Context Kernel v0.1.0 Verification Record

> **Historical record:** This document describes the pre-S0 `0.1.0` simulator MVP. It is retained for provenance only and is superseded by [`../s0_implementation_and_reaudit.md`](../s0_implementation_and_reaudit.md) for the current `0.2.1` S0 audit candidate.

**Final verification date:** 2026-08-16
**Runtime:** CPython 3.12.3
**Project version:** 0.1.0

## Final successful commands

The following commands were executed from `/home/ubuntu/aeon-context-kernel` after all implementation fixes.

```bash
uv sync --extra dev

.venv/bin/ruff format src tests examples
.venv/bin/ruff format --check src tests examples
.venv/bin/pytest --cov=context_kernel --cov=survival_bench --cov-report=term
.venv/bin/ruff check .
.venv/bin/mypy src
.venv/bin/python examples/basic_usage.py
.venv/bin/ckernel demo

.venv/bin/ckernel bench smoke --results-root results
.venv/bin/ckernel bench pilot --results-root results
.venv/bin/ckernel bench full --results-root results
.venv/bin/ckernel bench report results/context-survival-full-v1

for stage in smoke pilot full; do
  dir="results/context-survival-${stage}-v1"
  for trace in "$dir"/traces/*.jsonl; do
    .venv/bin/ckernel replay "$trace" >/dev/null || exit 1
  done
done
```

The project was initialized locally with the following repository command and was never pushed or deployed.

```bash
mkdir -p /home/ubuntu/aeon-context-kernel/{src/context_kernel/predicates,src/context_kernel/adapters,src/survival_bench/scenarios,tests,docs,examples,results}
cd /home/ubuntu/aeon-context-kernel
git init -q
git branch -m main
```

## Quality-gate results

| Gate | Final result |
|---|---:|
| Pytest | **115 passed** |
| Statement/branch coverage diagnostic | **93% total** |
| Ruff | **All checks passed** |
| Mypy strict source check | **No issues in 24 source files** |
| Example | External material remained in region D; enforcing filesystem policy blocked before effect; simulated effect count was 0. |
| Observe/warn/enforce demo | Observe executed, warn executed, and enforce blocked before the simulated effect. |

Coverage was used diagnostically. No numeric coverage threshold was configured; behavior-focused tests cover the requested trust, effect-boundary, receipt, replay, adapter, scenario, and reporting guarantees.

## Staged benchmark results

| Stage | Planned | Completed | Receipts | Traces | Full replays verified |
|---|---:|---:|---:|---:|---:|
| Smoke | 4 | 4 | 4 | 4 | 4 |
| Pilot | 48 | 48 | 48 | 48 | 48 |
| Full | 240 | 240 | 240 | 240 | 240 |
| **Total** | **292** | **292** | **292** | **292** | **292** |

All report tables were regenerated from each stage's saved `metrics.json`. Repeated regeneration preserved the canonical summary and survival-point JSON hashes. A final 292-trace replay sweep also passed after source formatting.

## Full deterministic simulator summary

| Arm | Runs | Survived | Violations | Right-censored | Blocked actions | False blocks |
|---|---:|---:|---:|---:|---:|---:|
| `flat` | 60 | 11 | 49 | 11 | 0 | 0 |
| `forced_compaction_baseline` | 60 | 1 | 59 | 1 | 0 | 0 |
| `admission_only` | 60 | 29 | 31 | 29 | 0 | 0 |
| `admission_plus_ledger` | 60 | 60 | 0 | 60 | 31 | 0 |

These are deterministic reference-simulator outcomes. They do not establish production superiority across actual model providers.

## Development repairs

Development gates caught and repaired an initially missing README required by the build backend, import-order and Python 3.12 lint findings, one accidentally collected pytest helper, a chart-title collision found by visual inspection, missing JSON Schema type stubs, and strict typing of benchmark matrix cells. The final commands above all pass after those repairs.
